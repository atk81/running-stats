import { ComingSoon } from "@/components/chrome/ComingSoon";

export default function MilestonesPage() {
  return (
    <ComingSoon
      eyebrow="Coming in Phase 4"
      title="Milestones"
      description="PRs, volume thresholds, streaks and weekly recaps appear here once detection is wired in Phase 4."
    />
  );
}
