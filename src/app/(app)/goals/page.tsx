import { ComingSoon } from "@/components/chrome/ComingSoon";

export default function GoalsPage() {
  return (
    <ComingSoon
      eyebrow="Coming in Phase 3"
      title="Goals"
      description="Goal editor with built-in time goals, volume target and custom presets lands in Phase 3."
    />
  );
}
